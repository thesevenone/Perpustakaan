@model IEnumerable<Perpustakaan.Models.BukuTabel>

@{
    ViewBag.Title = "Index";
}

<h2>Tambah Buku</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NamaPengarang)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.JudulBuku)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tersedia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NamaPengarang)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.JudulBuku)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tersedia)
        </td>
        <td>
           <!-- @Html.ActionLink("Edit", "Edit", new { id=item.ISBN }) |-->
            @Html.ActionLink("Details", "Details", new { id=item.ISBN }) 
           <!-- @Html.ActionLink("Delete", "Delete", new { id=item.ISBN })-->
        </td>
    </tr>
}

</table>
